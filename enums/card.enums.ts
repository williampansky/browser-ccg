/**
 * Enum values for the `Card.type`
 */
export enum CardType {
  Card = 'CARD',
  CARD = 'CARD',
  Minion = 'MINION',
  MINION = 'MINION',
  Spell = 'SPELL',
  SPELL = 'SPELL',
  Weapon = 'WEAPON',
  WEAPON = 'WEAPON',
}
