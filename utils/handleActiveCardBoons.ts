import { Ctx } from "boardgame.io";
import { GameState } from "../types";

const activeCardBoons = {
  recalculate: (G: GameState, ctx: Ctx) => {
    G.zones.forEach(z => {
      
    })
  }
}

export default activeCardBoons;