/**
 * Returns a single `card.mechanics` object pertaining to the provided param.
 */
const getMechanicObject = (symbol: string, json: object[]) => {

};

const getCardMechanics = (arr: string[]) => {};

export { getMechanicObject, getCardMechanics };
